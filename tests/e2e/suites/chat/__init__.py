"""Chat suite: the core conversation loop over the opencode TUI.

Covers single-turn hello and multi-turn memory (model selection, send, respond, and
context across turns). This is where most core-flow TUI tests live.
"""

from __future__ import annotations
